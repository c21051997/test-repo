name: Test Workflow
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."
      - run: sleep 10

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Testing..."
